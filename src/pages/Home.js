import React from 'react';
import BreedList from '../components/BreedList';

const Home = () => {
    return (
        <section className='home' id='home'>
            <BreedList />
        </section>
    );
};

export default Home;